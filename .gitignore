__pycache__/
*.pyc
*.pyo
.env
*.egg-info/
dist/
build/
.venv/
.pytest_cache/

# OS/editor files
.DS_Store

# Local scratch runs (regenerable)
scratch_*/

# Never commit copyrighted papers / reproductions (kept locally only, link instead)
*.pdf
latex_papers/

# Copyrighted published-figure scans (digitization inputs) — kept private only
/10.*.png
/ozgenFig3.png

# Verification scratch: figure-panel renders, reproductions of published panels,
# extracted PDF text, digitization intermediates, deck backups — NEVER commit
# (only pyMack-data overlays `overlay.png` + digitized-DATA CSVs are public)
**/_verify*.png
**/panels_v2/
**/_thesis_figs/
**/_p11_*.txt
**/_*.npy
*Backup.pptx
**/crop_*.png
**/overlay_fig15_full.png

# Process logs / scratch (not part of the public project)
*.log
automode_log.md
CODEX_*.md
AUDIT_REGENERATION_DOSSIER.md
*_RUN_LOG.md
*_AUDIT_LOG.md
PYMACK_COMPARISON_AUDIT.md

# Generated diagnostic outputs (regenerable; archived privately, not committed)
**/diagnostics/**/benchmark_*/
**/diagnostics/**/pymack_*/
**/diagnostics/**/smoke_*/

# Generated result outputs (regenerable; archived privately, not committed)
chapters/**/results/

# Internal provenance/comparison artifacts (regenerable; kept private)
**/trusted_manifests/

# Work-in-progress paper reproductions — kept private until validated
chapters/
baseline_snapshot/

# Local-only reference paper PDFs (copyrighted; never commit)
refPapers/
